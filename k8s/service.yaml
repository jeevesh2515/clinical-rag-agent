apiVersion: v1
kind: Service
metadata:
  name: clinical-workflow-service
  namespace: default
  labels:
    app.kubernetes.io/name: clinical-workflow-rag
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: clinical-workflow-rag
